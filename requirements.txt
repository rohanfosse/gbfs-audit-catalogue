# GBFS Audit Catalogue -- Streamlit Cloud / runtime dependencies
# Python >= 3.10
#
# Kept lean on purpose: Streamlit Cloud free tier has a build timeout
# and a 1 GB RAM ceiling. Jupyter, HF Hub CLI and the dev tooling
# (which are only needed when running the companion notebook or
# uploading to the Hub) are listed in pyproject.toml under the
# optional `[notebook]` and `[hub]` extras instead.

streamlit>=1.35.0
pandas>=2.0.0
numpy>=1.26.0
pyarrow>=14.0.0
